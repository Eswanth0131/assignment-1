CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
CrossEntropy-
Multihead
Attention ‘= On this page
Loss
CrossEntropyLoss
PReLU
CrossEntropyLoss.forward()
ReLU
class
ReLU6
torch.nn.CrossEntropyL(owesisc & Show Source
RReLU
size_average=None,
PyTorch Libraries
SELU ignore_index=-100, reduce=None,
torchaoG
reduction='mean',
CELU
torchrec G
Label_smoothing=00.) [source]
GELU torchft G
This criterion computes the cross TorchCodec G
Sigmoid
torchvision@
entropy loss between input logits
SiLU
Execulorch@
and target.
Mish PyTorch on XLA Devices 4
Softplus It is useful when training a
classification problem with C
Softshrink
classes. If provided, the optional
Softsign
argument weight should be a 1D
Tanh
Tensor assigning weight to each of
Tanhshrink
the classes. This is particularly
Threshold useful when you have an unbalanced
training set.
GLU
Softmin
The input is expected to contain the
Softmax unnormalized logits for each class
Softmax2d (which do not need to be positive or
sum to 1, in general). input has to be
LogSoftma
a Tensor of size (C’) for unbatched
X
input, (minibatch, C) or
AdaptiveL
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 1 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
(minibatch, U, dj), do,...,dK)
ogSoftmax
with kK > 1 for the K-dimensional
WithLoss
case. The last being useful for higher
BatchNor
dimension inputs, such as
mid
computing cross entropy loss per-
pixel for 2D images.
The target that this criterion expects
should contain either:
¢ Class indices in the range |0, C’)
where C’ is the number of
classes; if ignore_index is
specified, this loss also accepts
this class index (this index may
not necessarily be in the class
range). The unreduced (i.e. with
reduction setto 'none' ) loss
for this case can be described
as:
(a,y) =L={h,...,l
where z is the input, y is the
target, w is the weight, C’ is the
number of classes, and V
spans the minibatch dimension
as well as dj, ..., dy for the K-
dimensional case. If reduction
isnot 'none' (default
'mean' ), then
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 2 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
N
onal bn,
Note that this case is equivalent
to applying LogSoftmax onan
input, followed by NLLLoss .
e Probabilities for each class;
useful when labels beyond a
single class per minibatch item
are required, such as for
blended labels, label smoothing,
etc. The unreduced (i.e. with
reduction setto 'none' ) loss
for this case can be described
as:
(az,y)=L={h,...,
where z is the input, y is the
target, w is the weight, C’ is the
number of classes, and V
spans the minibatch dimension
as well as dj, ..., dy for the K-
dimensional case. If reduction
isnot 'none' (default
'mean' ), then
non Gf
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 3 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
Ldunai’ny Ul
® Note
The performance of this
criterion is generally better
when target contains class
indices, as this allows for
optimized computation.
Consider providing target
as class probabilities only
when a single class label
per minibatch item is too
restrictive.
Parameters:
e weight (7ensor, optional) -a
manual rescaling weight
given to each class. If given,
has to be a Tensor of size C.
e size_average (boo/,
optional) — Deprecated (see
reduction ). By default, the
losses are averaged over
each loss element in the
batch. Note that for some
losses, there are multiple
elements per sample. If the
field size_average is set to
False , the losses are
instead summed for each
minibatch. Ignored when
reduce is False . Nefauilt:
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 4 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
True
e ignore_index (int, optional)
— Specifies a target value
that is ignored and does not
contribute to the input
gradient. When
size_average is True, the
loss is averaged over non-
ignored targets. Note that
ignore_index is only
applicable when the target
contains class indices.
reduce (bool, optional) -
Deprecated (see
reduction ). By default, the
losses are averaged or
summed over observations
for each minibatch
depending on
size_average . When
reduce Is False, returns a
loss per batch element
instead and ignores
size_average . Default:
True
reduction (str, optional) —
Specifies the reduction to
apply to the output: 'none'
| 'mean' | 'sum'. 'none':
no reduction will be applied,
'mean' : the weighted mean
of the output is taken,
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 5 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
'sum' : the output will be
summed. Note:
size_average and reduce
are in the process of being
deprecated, and in the
meantime, specifying either
of those two args will
override reduction.
Default: 'mean'
label_smoothing (float,
optional) — A float in [0.0,
1.0]. Specifies the amount of
smoothing when computing
the loss, where 0.0 means no
smoothing. The targets
become a mixture of the
original ground truth and a
uniform distribution as
described in Rethinking the
Inception Architecture for
Computer Vision. Default:
0.0.
Shape:
¢ Input: Shape (C’), (NV, C)
Or (N, C, dy, dy, weey dx)
with K > 1 in the case of
K-dimensional loss.
Target: If containing class
indices, shape (), (JV) or
(N, di, do, ..., dx) with
K > 1 inthe case of kK-
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 6 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
dimensional loss where
each value should be
between |0, C’). The target
data type is required to be
long when using class
indices. If containing class
probabilities, the target
must be the same shape
input, and each value
should be between |0, 1].
This means the target data
type is required to be float
when using class
probabilities. Note that
PyTorch does not strictly
enforce probability
constraints on the class
probabilities and that it is
the user’s responsibility to
ensure target contains
valid probability
distributions (see below
examples section for more
details).
Output: If reduction is
‘none’, shape (), (JV) or
(N, dj, dbp, ..., dx) with
K > 1 inthe case of kK-
dimensional loss,
depending on the shape of
the input. Otherwise, scalar.
where:
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 7 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
C’ = number of classes
N = batch size
Examples
>>> # Example of target with c
>>> loss = nn.CrossEntropyLoss
>>> input = torch.randn(3, 5,
>>> target = torch.empty(3, dt
>>> output = loss(input, targe
>>> output. backward()
>>>
>>> # Example of target with c
>>> input = torch.randn(3, 5,
>>> target = torch.randn(3, 5)
>>> output = loss(input, targe
>>> output. backward()
® Note
When target contains
class probabilities, it should
conof ssoft liabesls—tthat
is, each target entry
should represent a
probability distribution over
the possible classes for a
given data sample, with
individual probabilities
between [@,1] andthe
total distribution summing
to 1. This is why the
softmax() functioins
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 8 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
applied to the target in
the class probabilities
example above.
PyTorch does not validate
whether the values
provided in target lie in
the range [0,1] or
whether the distribution of
each data sample sums to
1. No warning will be
raised and it is the user's
responsibility to ensure that
target contains valid
probability distributions.
Providing arbitrary values
may yield misleading loss
values and unstable
gradients during training.
Examples
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 9 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
>>> # Example of target with 1
>>> loss = nn.CrossEntropyLoss
>>> torch.manual_seed(283)
>>> input = torch.randn(3, 5,
>>> target = torch.randn(3, 5)
>>> # Provided target class pr
>>> target
tensor([[ @.7105, 0.4446, 2.
[-1.0496, -@.2753, -Q.
[ @.7551, 0.1003, 1.
>>> # Provided target class pr
>>> target.sum(axis=1)
tensor([2.8444, 0.2462, 0.8873
>>> # No error message and pos
>>> loss(input, target).item()
4.6379876136779785
>>>
>>> # Example of target with c
>>> # Use .softmax() to ensure
>>> target_new = target.softma
>>> # New target class probabi
>>> target_new
tensor([[@.1559, 0.1195, 0.583
[0@.0496, 0.1075, 0.099
[@.2607, 0.1355, 0.471
>>> # New target class probabi
>>> target_new. sum(axis=1)
tensor([1.0000, 1.0000, 1.0000
>>> loss(input, target_new).it
2.55349063873291
forward(input, target)
Runs the forward [source]
pass.
Return type:
Tensor
Rate this Page * %& *& & &
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 10 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
Send Feedback
Previous Next
< MSELoss CTCLoss
© Copyright PyTorch Contributors.
Docs Tutorials Resources
Access comprehensive Get in-depth tutorials for Find development
developer beginners and advanced resources and get your
documentation for developers questions answered
PyTorch View Tutorials View Resources
View Docs
Stay in touch for updates, event into,
and the latest news
First Name’ Last Name* Email* Select Cou SUBMIT
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 11 of 12
CrossEntropyLoss — PyTorch 2.9 documentation 12/7/25, 3:36 AM
> Ask Al
Policies Trademark Usage Privacy Policy
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html Page 12 of 12